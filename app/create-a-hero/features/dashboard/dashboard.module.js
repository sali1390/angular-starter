angular
    //Children modules utilizes dot notation to refer to them
    //Always add the array, whether there are dependencies or not, to CREATE the module
    .module('createAHero.dashboard',[

    ]);

